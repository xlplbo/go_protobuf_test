syntax = "proto3";

package protocol;

enum C2SCmd {
  None  = 0;  // 断开
  Login = 1;	// 登录
  Chat  = 2;  // 发送消息
}

message C2SLogin {
  string user   = 1;
  string passwd = 2;
}

message C2SChat {
  uint32 count     = 1;
  string context  = 2;
}
